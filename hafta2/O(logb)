#include <iostream>
#include <math.h>

using namespace std;

long pow(int x,int n){
if(n==0) return 1;
if(n==1) return x;
if(n%2==0){
    return pow(x*x,n/2);
}

else{
    return pow(x*x,n/2)*x;
}
}
int main()
{
    long sonuc=pow(3,40);

    cout << "sonuc= " << sonuc<< endl;
    return 0;
}
